from scripts.crop_target import CropTarget

def main():
    target = CropTarget()
    image = target.give_cropped_image(image, cam_param, shape, center, size, angle)

if __name__ == "__main__":
    main()
